---
description: 
globs: 
alwaysApply: true
---
---
globs: ["**/dedup/**/*.py", "**/scraper/**/*.py"]
alwaysApply: true
description: Email hashing for duplicate detection.
---
### Rule
Before duplicate lookup:
- Compute `hashlib.sha256(email.lower().encode()).hexdigest()`
- Use this hash as the lookup key in `lead_pool.jsonl`.
