---
description: "Standardized rule format."
globs: 
alwaysApply: true
---
---
globs: ["**/*.env", "**/docker-compose.yml"]
alwaysApply: true
description: API key management.
---
### Rule
- `.env` must be git-ignored.
- Docker-compose references `${CLEARBIT_KEY}`, `${HUNTER_KEY}`, `${ZOOMINFO_KEY}` only.
