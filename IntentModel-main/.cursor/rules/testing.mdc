---
description: 
globs: 
alwaysApply: true
---
---
globs: ["**/__tests__/**", "**/*.test.*"]
alwaysApply: true
description: Unit-test coverage for data-pipeline primitives.
---
### Rule
Minimum tests:
- `email_utils.test` → regex validation, MX lookup stub.
- `dedup.test` → SHA-256 hashing, duplicate detection.
- `enrich_retries.test` → simulate 429 and ensure retry then dead-letter.
- `json_schema.test` → validate sample outbox line against schema.
Target ≥ 80 percent statement coverage via Jest or PyTest.
